{
  "name": "llm-performance-testing",
  "version": "1.0.0",
  "description": "LLM performance testing framework with k6",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "echo 'No linter configured'",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "vitest": "^2.0.0"
  }
}
