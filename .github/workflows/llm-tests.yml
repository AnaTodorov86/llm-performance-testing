name: LLM Performance & Quality Tests

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  schedule:
    - cron: '0 9 * * 1' # Svaki ponedeljak u 09:00

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Install k6
        run: |
          sudo gpg -k
          sudo gpg --no-default-keyring --keyring /usr/share/keyrings/k6-archive-keyring.gpg --keyserver hkp://keyserver.ubuntu.com:80 --recv-keys C5AD17C747E3415A3642D57D77C6C491D6AC1D69
          echo "deb [signed-by=/usr/share/keyrings/k6-archive-keyring.gpg] https://dl.k6.io/deb stable main" | sudo tee /etc/apt/sources.list.d/k6.list
          sudo apt-get update
          sudo apt-get install k6

      - name: Run Baseline Test
        env:
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          TEST_SCENARIO: baseline
        run: k6 run --env GROQ_API_KEY=$GROQ_API_KEY --env TEST_SCENARIO=$TEST_SCENARIO tests/performance_test.js

      - name: Run Quality Test
        env:
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
        run: k6 run --env GROQ_API_KEY=$GROQ_API_KEY tests/quality_test.js

      - name: Run Consistency Test
        env:
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
        run: k6 run --env GROQ_API_KEY=$GROQ_API_KEY tests/consistency_test.js

      - name: Upload Test Reports
        if: always() # Čak i ako testovi fejliuju, sačuvaj izveštaj
        uses: actions/upload-artifact@v4
        with:
          name: k6-test-reports
          path: reports/